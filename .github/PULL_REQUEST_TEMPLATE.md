## Summary

<!-- What does this PR do? Keep it to 1-2 sentences. -->

## Changes

<!-- Bullet list of changes -->
-
-
-

## Testing

<!-- How was this tested? -->
- [ ] Ran `python -m pytest tests/ -v` -- all tests pass
- [ ] Tested manually with `python main.py --format json --out test.json`
- [ ] Tested on: <!-- OS and Python version -->

## Checklist

- [ ] All existing tests pass
- [ ] New tests added for new functionality
- [ ] No third-party dependencies added (pure Python stdlib only)
- [ ] Module docstrings updated if applicable
- [ ] CLI `--help` updated if new flags added
- [ ] Dashboard view added if new artifact type (nav item + renderer + view div)
- [ ] `--full` flag updated to include new feature flags
